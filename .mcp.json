{
  "mcpServers": {
    "zukai-jev": {
      "command": "node",
      "args": ["mcp/server.js"],
      "env": {
        "AI_GATEWAY_API_KEY": "${AI_GATEWAY_API_KEY:-}",
        "JEV_MODEL": "${JEV_MODEL:-typesafe-ai/jev}"
      }
    }
  }
}
